# JPA and PostgreSQL

Read [Postgres and JPA](postgres-jpa-reference.md), then [wallet and ledger modeling](wallet-ledger-reference.md). Keep relational constraints and transaction behavior explicit even when an ORM generates SQL.
