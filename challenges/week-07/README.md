# Week 7 Challenges — Collections and Streams

- Sort expenses by amount and then by date using a comparator.
- Filter a collection, map values, and compute a total; predict the result first.
- Find one optional value without calling `get()` blindly.
- Rewrite a stream pipeline as a loop and compare which is easier to read.
