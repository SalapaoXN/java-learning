# Week 5 Challenges — OOP

- Model two payment methods behind one interface and call them through the interface type.
- Replace inheritance with composition where the relationship is “uses” rather than “is a”.
- Add an enum for a small fixed set of order states.
- Explain which method an overridden call dispatches to at runtime.
